[package]
name = "infinite-craft"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.80"
clap = { version = "4.4.18", default-features = false, features = [
    "std",
    "usage",
    "help",
    "derive",
] }
